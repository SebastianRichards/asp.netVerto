@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{

}

@model VertoTest.Models.ContentModel



@{
    ViewData["Title"] = "Admin";
}

@{
    int uniqueIdentifier = 0; // Declare the variable outside the loop
}

<!-- Success Message Display -->
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert" style="background-color: #d4edda; border-color: #c3e6cb; color: #155724; padding: 12px; margin: 10px 0; border-radius: 5px;">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close" style="float: right; background: none; border: none; font-size: 18px; cursor: pointer;">&times;</button>
    </div>
}

@if (TempData["InfoMessage"] != null)
{
    <div class="alert alert-info alert-dismissible fade show" role="alert" style="background-color: #d1ecf1; border-color: #bee5eb; color: #0c5460; padding: 12px; margin: 10px 0; border-radius: 5px;">
        @TempData["InfoMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close" style="float: right; background: none; border: none; font-size: 18px; cursor: pointer;">&times;</button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert" style="background-color: #f8d7da; border-color: #f5c6cb; color: #721c24; padding: 12px; margin: 10px 0; border-radius: 5px;">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close" style="float: right; background: none; border: none; font-size: 18px; cursor: pointer;">&times;</button>
    </div>
}

<!-- Admin Controls -->
<div class="admin-controls" style="text-align: center; margin: 20px 0; padding: 20px; background-color: #f8f9fa; border-radius: 5px;">
    <h2 style="margin-bottom: 15px; color: #333;">Admin Controls</h2>
    
    <form method="post" action="/Home/SeedInitialData" style="display: inline; margin-right: 10px;">
        <button type="submit" class="seed-data-button" style="background-color: #28a745; color: white; border: none; padding: 12px 24px; border-radius: 5px; font-size: 16px; cursor: pointer; margin: 5px;">
            🌱 Create Initial Data
        </button>
    </form>
    
    <form method="post" action="/Home/ResetToDefault" style="display: inline;" onsubmit="return confirm('Are you sure you want to reset all content to default values? This action cannot be undone.');">
        <button type="submit" class="reset-default-button" style="background-color: #dc3545; color: white; border: none; padding: 12px 24px; border-radius: 5px; font-size: 16px; cursor: pointer; margin: 5px;">
            🔄 Reset to Default
        </button>
    </form>
    
    <p style="font-size: 14px; color: #666; margin-top: 10px;">
        <strong>Create Initial Data:</strong> Sets up content for first-time use<br>
        <strong>Reset to Default:</strong> Restores all text to Lorem Ipsum and default images
    </p>
</div>

<div class="text-center">
    
    <section>
        <div class="carousel" data-carousel>
            <button class="carousel-button prev" data-carousel-button="prev">&#8701;</button>
            <button class="carousel-button next" data-carousel-button="next">&#8702;</button>
            <ul data-slides>
                <li class="slide" data-active>
                    <img src="@Url.Content("~/static/birdbanner3.png")" alt="img1" />
                </li>
                <li class="slide">
                    <img src="@Url.Content("~/static/birdbanner2.jpeg")" alt="img2" />
                </li>
                <li class="slide">
                    <img src="@Url.Content("~/static/birdbanner1.jpeg")" alt="img3" />
                </li>


            </ul>
        </div>
    </section>

    <div class="grid-x">
        <div class="grid-x grid-container home-grid-one small-12 grid-one-recenter">
            <div class="small-12 medium-6 large-3 row1">
                <div class="home-container-title">New Products</div>


                @foreach (var content in ViewBag.contents)
                {
                    if (content.Name == "text-box-1")
                    {
                        <div class="image-row-1">
                            <img class="image-row-1" src="@Url.Content("~/" + content.PhotoName)" />
                            
                        </div>
                        <p>@content.Text</p>
                        uniqueIdentifier = content.Id;
                    }
                }


                <button class="home-button" type="button">New Products</button>
                <form method="post" action="/home/AdminEdit">
                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                    <button class="edit-button" type="submit">Edit</button>
                </form>

            </div>

            <div class="small-12 medium-6 large-3 row1">
                <div class="home-container-title">Field Events</div>
                
                @foreach (var content in ViewBag.contents)
                {
                    if (content.Name == "text-box-2")
                    {
                        <div class="image-row-1">
                            <img class="image-row-1" src="@Url.Content("~/" + content.PhotoName)" />

                        </div>
                        uniqueIdentifier = content.Id;
                        <p>@content.Text</p>
                        }
                }
                <button class="home-button" type="button">View Events</button>
                <form method="post" action="/home/AdminEdit">
                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                    <button class="edit-button" type="submit">Edit</button>
                </form>
            </div>
            <div class="small-12 medium-6 large-3 row1">
                <div class="home-container-title">Latest News</div>
                
                @foreach (var content in ViewBag.contents)
                {
                    if (content.Name == "text-box-3")
                    {

                        <div class="image-row-1">
                            <img class="image-row-1" src="@Url.Content("~/" + content.PhotoName)" />

                        </div>
                        uniqueIdentifier = content.Id;
                        <p>@content.Text</p>
                        }
                }
                <button class="home-button" type="button">Read Article</button>
                <form method="post" action="/home/AdminEdit">
                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                    <button class="edit-button" type="submit">Edit</button>
                </form>
            </div>
            <div class="small-12 medium-6 large-3 row1">
                <div class="home-container-title">Gallery</div>
                
                @foreach (var content in ViewBag.contents)
                {
                    if (content.Name == "text-box-4")
                    {
                        <div class="image-row-1">
                            <img class="image-row-1" src="@Url.Content("~/" + content.PhotoName)" />

                        </div>
                        uniqueIdentifier = content.Id;
                        <p>@content.Text</p>}
                }
                <button class="home-button" type="button">View Gallery</button>
                <form method="post" action="/home/AdminEdit">
                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                    <button class="edit-button" type="submit">Edit</button>
                </form>
            </div>


        </div>


        <div class="small-12 home-grid-two grid-container home-grid-one">
            <p class="special-offer-text">Special Offers</p>
            <div class="grid-x text-image-box">
                <div class="large-4 medium-12 image-center row2-outer-div" style="position: relative">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-5")
                        {
                            uniqueIdentifier = content.Id;
                            <div class="row2-div-holder">
                                <img src="@Url.Content("~/" + content.PhotoName)" style="width: 100%;"/>
                                <div class="row2-image-text">
                                    <p class="row2-text">@content.Text</p>
                                    <p class="row2-text-bold">@content.Text2</p>

                                    <div class="row2-admin-edit">
                                        <form method="post" action="/home/AdminEdit">
                                            <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                            <button class="edit-row-2" type="submit">Edit</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                            


                        }

                    }
                </div>
                <div class="large-4 medium-12 image-center row2-outer-div" style="position: relative">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-6")
                        {
                            uniqueIdentifier = content.Id;
                            <div class="row2-div-holder">
                                <img src="@Url.Content("~/" + content.PhotoName)" style="width: 100%;"/>
                                <div class="row2-image-text">
                                    <p class="row2-text">@content.Text</p>
                                    <p class="row2-text-bold">@content.Text2</p>
                                    <div class="row2-admin-edit">
                                        <form method="post" action="/home/AdminEdit">
                                            <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                            <button class="edit-row-2" type="submit">Edit</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                            


                        }

                    }
                </div>
                <div class="large-4 medium-12 image-center row2-outer-div" style="position: relative">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-7")
                        {
                            uniqueIdentifier = content.Id;
                            <div class="row2-div-holder">
                                <img src="@Url.Content("~/" + content.PhotoName)" style="width: 100%;"/>
                                <div class="row2-image-text">
                                    <p class="row2-text">@content.Text</p>
                                    <p class="row2-text-bold">@content.Text2</p>
                                    <div class="row2-admin-edit">
                                        <form method="post" action="/home/AdminEdit">
                                            <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                            <button class="edit-row-2" type="submit">Edit</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                            


                        }

                    }
                </div>
            </div>
            <div class="view-offer-button-admin">
                <button class="home-button">View All Offers</button>
            </div>
        </div>

        <div class="small-12 home-grid-one home-grid-three grid-container">
            <p class="row3-title-text">Product Categories</p>
            <div class="grid-x grid-container center-div home-grid-one">
                <div class="cell large-3 medium-6 small-6 image-test">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-8")
                        {
                            uniqueIdentifier = content.Id;

                            <img class="image-test" src="@Url.Content("~/" + content.PhotoName)" />
                            <p>@content.Text</p>
                            <div class="row3-admin-edit">
                                <form method="post" action="/home/AdminEdit">
                                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                    <button class="edit-row-2" type="submit">Edit</button>
                                </form>
                            </div>

                        }

                    }
                </div>
                <div class="cell large-3 medium-6 small-6 image-test">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-9")
                        {
                            uniqueIdentifier = content.Id;
                            <img class="image-test" src="@Url.Content("~/" + content.PhotoName)" />
                            <p>@content.Text</p>
                            <div class="row3-admin-edit">
                                <form method="post" action="/home/AdminEdit">
                                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                    <button class="edit-row-2" type="submit">Edit</button>
                                </form>
                            </div>

                        }

                    }
                </div>
                <div class="cell large-3 medium-6 small-6 image-test">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-10")
                        {
                            uniqueIdentifier = content.Id;
                            <img class="image-test" src="@Url.Content("~/" + content.PhotoName)" />
                            <p>@content.Text</p>
                            <div class="row3-admin-edit">
                                <form method="post" action="/home/AdminEdit">
                                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                    <button class="edit-row-2" type="submit">Edit</button>
                                </form>
                            </div>
                        }

                    }
                </div>
                <div class="cell large-3 medium-6 small-6 image-test">
                    @foreach (var content in ViewBag.contents)
                    {
                        if (content.Name == "text-box-11")
                        {

                            uniqueIdentifier = content.Id;
                            <img class="image-test" src="@Url.Content("~/" + content.PhotoName)" />
                            <p>@content.Text</p>
                            <div class="row3-admin-edit">
                                <form method="post" action="/home/AdminEdit">
                                    <input type="hidden" name="idNumber" value="@uniqueIdentifier" />
                                    <button class="edit-row-2" type="submit">Edit</button>
                                </form>
                            </div>

                        }

                    }
                </div>
            </div>
        </div>
    </div>


</div>


